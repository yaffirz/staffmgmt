# Changelog 0026 — Housekeeping: remove deprecated frontend/, refresh CLAUDE.md

- **Timestamp:** 2026-07-16 (AST, UTC-4)
- **Requested by:** Arif
- **Task:** Clear session loose ends: delete the deprecated `frontend/` folder
  (CLAUDE.md long said it "should be deleted") and bring CLAUDE.md's state sections
  up to date.
- **Status:** Applied on disk (docs + file removal — no runtime impact).

## What changed
- **Removed the deprecated `frontend/` folder** (27 files) — the old Flutter copy
  that "previously caused sync bugs." The canonical frontend is `staff_frontend/`.
  Confirmed nothing references it (docker-compose builds only db + backend; no
  cross-imports). Reversible via git.
- **CLAUDE.md refreshed:**
  - Removed the "frontend/ should be deleted" note (now done).
  - Roles line updated to include IT + multi-role.
  - "Current state" rewritten to reflect Phase 2, staff notes, IT/multi-role,
    Phase 3, notifications + all 5 triggers, and the audit mini-console.
  - "Next planned work" replaced (all prior items done) with candidate follow-ups.

## Files touched
- Deleted: frontend/** (27 files)
- Modified: CLAUDE.md

## Verification
- `git rm -r frontend/` staged 27 deletions; working tree no longer has frontend/.
- No build/runtime impact (staff_frontend is independent; backend unaffected).

## Deployment
- Built: no. Deployed to production: no.

## Rollback
- `git revert` the housekeeping commit (restores frontend/ and the old CLAUDE.md).
