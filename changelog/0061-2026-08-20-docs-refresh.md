# Changelog 0061 — Documentation refresh (current state, bugs/fixes, stability)

- **Timestamp:** 2026-08-20 (AST, UTC-4)
- **Requested by:** Arif
- **Task:** Ensure the changelog folder, CLAUDE.md and other docs are up to date
  and document all known bugs, their fixes, all changes/updates, and platform
  stability.
- **Status:** Applied (docs only).

## What changed
- **Changelog folder:** already complete — one file per change, contiguous
  **0001–0060** (verified: 60 files, no gaps). This entry (0061) records the doc
  refresh itself.
- **`CLAUDE.md`:** "Current state" extended with every feature through 0060
  (country management, employee filters + UX, universal positions, per-employee /
  foodmall brand, email-unavailable flag, global top bar, force-password-change,
  account suspension, IT store management, username-or-email login). Added a
  **"Known bugs fixed (Aug 2026)"** section (the "replace child rows" 500 family +
  the delete-user, filter-dialog, foodmall-picker and eye-icon fixes) and a
  **"Stability"** section. Refreshed "Next planned work".
- **`README.md`:** "Current status" (all committed, latest changelog 0060, stable),
  feature list extended to 0047–0060, "Problems encountered & solutions" gains the
  Aug bug fixes, and "Outstanding / next steps" refreshed (0040/0041 done; St Kitts
  / Doubles / Trincity data noted).
- **`SESSION_HANDOFF.md`:** banner marking it a point-in-time snapshot (~0030),
  superseded by CLAUDE.md + changelog through 0060.

## Note on stability
Stable. All schema changes this cycle are non-destructive
(`ADD COLUMN IF NOT EXISTS`); each change was verified before commit (backend via
the running API, frontend via `flutter analyze` + `flutter build web`, key flows
in-browser). No data loss; no `down -v`.

## Rollback
- Revert the doc edits; delete this changelog file.
