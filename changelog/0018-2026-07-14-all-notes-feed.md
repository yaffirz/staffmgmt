# Changelog 0018 — "Staff Notes" tile → all-notes feed

- **Timestamp:** 2026-07-14 (AST, UTC-4)
- **Requested by:** Arif
- **Task:** The dashboard "Staff Notes / Performance logs" tile was a dead stub
  (snackbar "coming in a later step"). Make it a real feed of every note the user
  is allowed to see across all staff.
- **Status:** Backend applied + verified via curl; frontend applied,
  `flutter analyze` clean (1 pre-existing info note). Pending a frontend restart.

## Backend (verified)
- `GET /api/v1/staff/notes/all` (Area Manager / HR / Admin / Super Admin) — every
  note visible to the caller (same visibility rule) across staff they can view,
  newest first, each with `employee_name`. Path is `/notes/all` to avoid colliding
  with `/staff/{employee_id}`.
- New `NoteFeedItem` schema; `_viewable_employees()` helper (Admin/HR/Super Admin =
  all; Area Manager = cluster staff).
- Verified: am_pizza sees own 3 (on "1234"); hr_test sees only the HR-shared note;
  superadmin sees all 3; `/staff/3` still returns 200 (no route collision).

## Frontend
- `models/staff_note.dart`: optional `employeeName` (present on the feed).
- `services/staff_service.dart`: `allStaffNotes()`.
- `screens/all_notes_screen.dart` (new): feed list — employee name, note text,
  visibility chip, author · date; each row taps through to that staffer's page.
- `screens/dashboard_screen.dart`: the "Staff Notes" tile (HR, Area Manager,
  viewer) now navigates to the feed (`_Dest.allNotes`) instead of the dead snackbar.

## Files touched
- backend/app/schemas/notes.py — NoteFeedItem
- backend/app/api/routes/notes.py — /notes/all + _viewable_employees
- staff_frontend/lib/models/staff_note.dart — employeeName
- staff_frontend/lib/services/staff_service.dart — allStaffNotes()
- staff_frontend/lib/screens/all_notes_screen.dart — NEW
- staff_frontend/lib/screens/dashboard_screen.dart — tile → feed

## Verification
- Backend via curl (above). `flutter analyze` → 0 errors/warnings.
- Pending frontend restart, then confirm the tile opens the feed and rows link to
  the staff page, per role.

## Deployment
- Built: no. Backend restarted (volume+reload). Frontend restart required.
- Deployed to production: no

## Rollback
- Revert the tile dest, remove all_notes_screen.dart + the feed endpoint/schema.
